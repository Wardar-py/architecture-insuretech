apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: scaletestapp
  namespace: default
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: scaletestapp
  podMetricsEndpoints:
    - port: "8080"
      path: /metrics